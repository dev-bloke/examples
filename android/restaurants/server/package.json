{
  "name": "server",
  "version": "1.0.0",
  "description": "Local REST / JSON server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx json-server db.json --host=0.0.0.0 --port=8080"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
