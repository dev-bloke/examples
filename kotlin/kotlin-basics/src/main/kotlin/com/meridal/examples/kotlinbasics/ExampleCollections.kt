package com.meridal.examples.kotlinbasics

class ExampleCollections {
    val list = listOf(1, 2, 3, 4)
    val map = mapOf("first" to "one", "second" to "two")
    val distinct = setOf(1, 2, 3, 4, 1, 2)
}